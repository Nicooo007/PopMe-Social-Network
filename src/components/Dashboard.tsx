import Feed from "./Feed";

export default function Dashboard() {

  return (

    // Feed that contains all the post or reviews
    <div className="bg-[#1e1c24] min-h-screen flex justify-center items-start p-6">
      <Feed />
    </div>
  );
}
